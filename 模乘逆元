#include<bits/stdc++.h> 

using namespace std;
int res(int n,int m)
{
    for(int i = 1; i < m; i++)
        if((n * i) % m == 1 ) return i;
    return -1;
}
int main( )
{
    int n,m;
    cin>>n>>m;
    cout<<res(n,m);
    return 0;
}
