#include<bits/stdc++.h> 

using namespace std;

int main( )
{
    int m,n,s;
    cin>>m;
    for(int i = 1; i <= m; i++)
    {
        n = i;
        s = 0;
        while(n)
        {
            s += pow(n%10,3);
            n = n/10;
        }
        if(s==i) printf("%d ",i);
        
    }

    return 0;
}
