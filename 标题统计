#include <bits/stdc++.h>
using namespace std;
int main()
{
  string s;
  int i=0,count=0;
  getline(cin,s);
  while(i<s.size()){
    if(s[i]!=' ')count++;
    i++;
  }
  cout<<count<<endl;
  return 0;
}
