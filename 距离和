#include <bits/stdc++.h>
using namespace std;
int main()
{
  // 请在此输入您的代码
  string s="LANQIAO";
  int ans=0;
  for(int i=0;i<s.size()-1;i++)
    for(int j=i+1;j<s.size();j++)
      {
          ans+=abs((int)(s[i]-s[j]));
      }
  cout<<ans;
  return 0;
}
