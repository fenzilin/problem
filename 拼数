#include<bits/stdc++.h>
using namespace std;
bool guize(string a,string b){
    return a+b>b+a;
}
int main() { 
 string array[20];int n;
 cin>>n;
for(int i=0;i<n;i++)
   cin>>array[i];
   sort(array,array+n,guize);
   for(int i=0;i<n;i++)
   cout<<array[i];
 return 0;
}
