#include <iostream>
#include <cmath>
using namespace std;
float hl(double a,double b,double c)
{
  float s = (a+b+c)/2;
  float A = sqrt(s*(s-a)*(s-b)*(s-c));
  return A;
}
int main()
{
  // 请在此输入您的代码
  printf("%.2f",hl(52.1,33.4,68.2)+hl(68.2,57.2,71.9)+hl(71.9,51.9,43.5));
  return 0;
}
